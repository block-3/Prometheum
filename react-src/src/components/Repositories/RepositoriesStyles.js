let styles = StyleSheet.create({
  Repositories: {
    borderWidth: "1px",
    borderColor: "black",
    borderStyle: "solid"
    }
  });

export const style = StyleSheet.flatten(styles.Repositories);
